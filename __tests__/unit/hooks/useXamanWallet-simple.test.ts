/**
 * Simple test for useXamanWallet hook to debug Jest issues
 */

import { describe, it, expect } from '@jest/globals';

describe('useXamanWallet Hook - Simple Test', () => {
  it('should have a simple passing test', () => {
    expect(true).toBe(true);
  });
});
